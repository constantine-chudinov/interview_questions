/**
 * Check whether array has any two elements i and j for which arr[i] + arr[j] = value
 * @param {number[]} array of unique numbers like [2, 7, 5]
 * @param {number} sumValue sum of two items
 * @returns {boolean}
 */
var checkArrayForSum = (array, sumValue) => {  
    // ... Your code here
}

module.exports = checkArrayForSum;